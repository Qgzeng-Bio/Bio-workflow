# Raw data

This directory contains read-only project inputs or links to protected external storage.

Allowed: original inputs, immutable references, and documented read-only links listed in `config/Input_Manifest.tsv`.

Forbidden: analysis outputs, modified derivatives, logs, temporary indexes, result tables, figures, and manuscript files.

Do not add raw data to Git. Keep only this README and the project manifests under version control.
